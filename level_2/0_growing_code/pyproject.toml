[project]
name = "growing-code"
version = "0.1.0"
description = "first python project from 42"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "mypy>=1.20.0",
]
